[package]
name = "third_party"
version = "0.1.0"
authors = ["Tim Blakely <tim.blakely@gmail.com>"]
edition = "2021"


[dependencies]
# Temporary until https://github.com/burtonageo/ang/pull/1 is accepted/released
ang = {path = "../../ang", default-features = false, features=[]}
cortex-m = "0.7.2"
scopeguard = {version="1.1.0", default-features=false}

[lib]
test = false
bench = false
